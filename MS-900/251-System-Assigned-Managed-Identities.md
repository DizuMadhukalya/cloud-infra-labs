# System-Assigned Managed Identities

Created automatically when enabled on resource.

## Characteristics
- Linked to resource lifecycle
- Deleted when resource deleted
- One identity per resource

## Example

Azure App Service enabled with system-assigned identity.
